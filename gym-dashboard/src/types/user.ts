export type User = {
  user_id?: number;
  full_name: string;
  gender: string;
  birthday?: string;
  age?: number;
  height?: number;
  weight?: number;
  email: string;
  phone_number: string;
  aim?: string;
  activity_extent?: string;
  profile_picture?: string;
  join_date?: string; // Add the join_date field
};
